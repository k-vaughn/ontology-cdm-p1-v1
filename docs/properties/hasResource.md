# hasResource

Specifies a Resource that participates in the State. This identifies the condition of the object that plays a resource role (i.e. is used, consumed, etc.) when the state’s status is active.

**Domain**: [TerminalResourceState](../classes/TerminalResourceState.md)

**Range**: [Resource](../classes/Resource.md)

**IRI**: `https://w3id.org/citydata/part1/v1/hasResource`
