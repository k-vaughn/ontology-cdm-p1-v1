# componentOf

Specifies a part-whole relationship between objects where the component is a part that is defined based on actual boundaries. The parts are often also defined according to distinct functions. For example, a headlight is a componentOf a car. 

**IRI**: `https://w3id.org/citydata/part1/v1/componentOf`
